// config.js - Configurações globais (chaves de API, etc.)
// Em produção, use variáveis de ambiente para proteger chaves.

export const CONFIG = {
    EMAILJS_PUBLIC_KEY: "YOUR_PUBLIC_KEY", // Substitua pela chave real
    EMAILJS_SERVICE_ID: "YOUR_SERVICE_ID",
    EMAILJS_TEMPLATE_ID: "YOUR_TEMPLATE_ID",
    API_BASE_URL: "https://api.skyscanner.com", // Exemplo para APIs reais
    MOCK_MODE: true // true para usar dados simulados; false para APIs reais
};